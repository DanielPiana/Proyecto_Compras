name: proyectocompras
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  supabase_flutter: ^2.8.4
  flutter_dotenv: ^5.2.1
  image_picker: ^1.0.4
  file_picker: ^10.3.0
  flutter_slidable: ^3.0.0
  flutter_speed_dial: ^7.0.0
  flutter:
    sdk: flutter
  sqflite: ^2.2.7

  path: ^1.8.3
  shared_preferences: ^2.4.0
  flutter_localizations:
    sdk: flutter
  intl: any
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  sqflite_common_ffi: ^2.3.3 # Mantener esto en "dev_dependencies" para pruebas en escritorio


flutter:
  assets:
    - .env
  generate: true
  uses-material-design: true
